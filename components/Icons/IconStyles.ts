import { StyleSheet } from "react-native";

const iconStyles = StyleSheet.create({
  tabBarIconStyle: {
    backgroundColor: "#2d6850",
    paddingVertical: 2,
    paddingHorizontal: 4,
    borderRadius: 15,
  },
});

export default iconStyles;
